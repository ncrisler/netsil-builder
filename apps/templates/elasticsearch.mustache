{
  "id": "{{ elasticsearch.app_id }}",
  "container": {
     "type": "DOCKER",
     "volumes": [
{{#elasticsearch.production}}
       {
         "containerPath": "es_data",
         "mode": "RW",
         "persistent": {
           "size": 100
         }
       },
       {
         "containerPath": "/usr/share/elasticsearch/data",
         "hostPath": "es_data",
         "mode": "RW"
       },
       {
        "hostPath": "/var/log/netsil",
        "containerPath": "/var/log/netsil",
        "mode": "RW"
       }
{{/elasticsearch.production}}
     ],
     "docker": {
         "network": "BRIDGE",
         "image": "{{ elasticsearch.uri_namespace }}/elasticsearch:{{ elasticsearch.netsil_service_tag }}",
         "forcePullImage": false,
         "portMappings": [
             {"containerPort": {{ elasticsearch.http_port }}, "hostPort": {{ elasticsearch.http_port }}, "protocol":"tcp"},
             {"containerPort": {{ elasticsearch.transport_port }}, "hostPort": {{ elasticsearch.transport_port }}, "protocol":"tcp"}
         ]
     }
 },
  "cpus": {{ elasticsearch.cpus }},
  "mem": {{ elasticsearch.mem }},
  "acceptedResourceRoles": ["{{ elasticsearch.role }}"],
  "constraints": [
      ["hostname", "UNIQUE"]
  ],
  "upgradeStrategy" : {
      "minimumHealthCapacity" : 0,
      "maximumOverCapacity" : 0
  },
{{#elasticsearch.private}}
  "uris": ["file:///etc/docker.tar.gz"],
{{/elasticsearch.private}}
  "instances": {{ elasticsearch.instances }},
  "residency": {
    "taskLostBehavior": "WAIT_FOREVER"
  }
}